// Values mirrored from index.html so tests can reference storage keys etc.
// (These are stable identifiers, not logic — the logic itself is always called
//  live via page.evaluate, never re-implemented here.)
export const SB_AUTH_KEY = 'sb-xsmnfcmtbpeaccnyinkr-auth-token';
export const OUTBOX_KEY = 'ink_outbox';
